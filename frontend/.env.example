# API 后端地址配置
# 根据部署环境选择对应的配置

# 本地开发（默认）
REACT_APP_API_URL=http://localhost:5001

# 局域网访问（同Wi-Fi用户访问）
# REACT_APP_API_URL=http://192.168.255.10:5001

# 生产环境（部署到服务器后填写服务器地址）
# REACT_APP_API_URL=https://your-domain.com

# 使用方法：
# 1. 复制此文件为 .env.local
# 2. 取消注释需要使用的配置
# 3. 重启前端服务 (npm start)
